import {HashRouter as HRouter, Link as Linky, Route as Route66} from "react-router-dom"
import React from "react"

export const Route         = Route66;
export const Link          = Linky;
export const HashRouter    = HRouter;
export const UserContext   = React.createContext(null);

